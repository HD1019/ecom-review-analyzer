# 阿里云 API 市场上架指南

## 前置条件

1. 阿里云账号（实名认证）
2. API 已部署到公网可访问的服务器

---

## 第一步：部署 API 到公网

### 方式一：阿里云 ECS（推荐）

```bash
# 1. 购买 ECS 实例（CentOS 7 / Ubuntu 20.04，最低 1核2G）
# 2. SSH 登录后安装 Docker
curl -fsSL https://get.docker.com | bash

# 3. 上传项目文件到服务器
scp -r ./* root@你的ECS公网IP:/app/

# 4. 构建并运行
cd /app
docker build -t review-analyzer .
docker run -d \
  -p 8000:8000 \
  -e LLM_API_KEY=sk-你的key \
  -e ADMIN_API_KEY=你的管理员密钥 \
  --restart always \
  review-analyzer

# 5. 配置安全组：放行 8000 端口
# 6. 验证：curl http://你的ECS公网IP:8000/
```

### 方式二：阿里云函数计算 FC

使用 Custom Container 运行，冷启动约 1-3 秒。

---

## 第二步：创建 API 网关实例

1. 登录 [阿里云 API 网关控制台](https://apigateway.console.aliyun.com/)
2. 创建实例 → 选择「共享实例」(免费) 或「专享实例」
3. 创建 API 分组：
   - 分组名称：`电商评论分析`
   - 描述：`AI驱动的电商用户评论结构化分析服务`
   - BasePath：`/api/v1`

---

## 第三步：导入 API 定义

1. 进入 API 分组 → 点击「导入 API」
2. 选择「Swagger / OpenAPI 3.0」
3. 上传 `openapi.json` 文件（本项目已生成）
4. 系统自动创建以下路由：
   - `POST /api/v1/analyze-reviews` — 评论分析
   - `GET /` — 健康检查
   - `POST /admin/generate-key` — 生成密钥（建议隐藏）
   - `GET /admin/list-keys` — 密钥列表（建议隐藏）
5. 编辑每个 API →「后端配置」页面：
   - 后端地址：`http://你的ECS公网IP:8000`（或 FC 域名）
   - 后端超时：`120000`（120秒，因为 LLM 可能较慢）

---

## 第四步：配置认证与限流

### 配置 APP 认证（用户密钥验证）

1. 进入「API 分组」→「发布」→「环境」选择「线上」
2. 进入「调用 API」→「应用管理」→「创建应用」
3. 应用创建后获取 AppKey / AppSecret
4. 进入「API 分组」→「授权」→ 将 API 授权给该应用

### 配置流量控制

| 策略名称 | 阈值 |
|---|---|
| 基础流控 | 100次/分钟 |
| 单用户流控 | 根据套餐：20/500/2000/10000 次/月 |

---

## 第五步：上架 API 市场

1. 登录 [阿里云 API 市场](https://market.console.aliyun.com/)
2. 点击「发布 API 服务」
3. 填写以下信息（详见下方清单）
4. 提交审核（1-3个工作日）

---

## 市场发布内容清单

### 基本信息

| 字段 | 内容 |
|---|---|
| API 名称 | E-commerce Review Analyzer（电商评论智能分析） |
| 所属分类 | AI 应用 > 文本分析 |
| 服务简介 | 基于大语言模型的电商评论智能分析API，自动提取优缺点及严重程度、生成用户画像、情感分布和改进建议 |
| 服务版本 | 1.1.0 |
| 交付方式 | API 交付 |

### 服务介绍（详细描述）

```markdown
## 产品介绍

AI电商评论分析API 调用先进的大语言模型，对批量用户评论进行结构化分析。无需逐条阅读数百条评论，几秒内即可获得整合报告。

## 核心功能

- **优缺点提取**：每条发现均附带用户原话引用，确保有据可查
- **严重程度标注**：缺点标注高/中/低三个等级，帮助排出修复优先级
- **用户画像生成**：模型综合分析后输出50字以内的典型买家画像
- **情感分布量化**：正面/负面/中性比例一目了然
- **改进建议输出**：从所有反馈中提炼出一条最有效的产品改进建议

## 适用场景

| 场景 | 说明 |
|---|---|
| 新品上市复盘 | 分析早期评论，决定下批次优化方向 |
| 竞品情报调研 | 抓取竞品评论，挖掘对方产品弱点 |
| 客服问题预判 | 识别高频痛点，在退款潮前介入 |
| 营销文案优化 | 提取用户好评高频词，优化商品详情页 |
| 供应链质量管控 | 通过负面评论早期发现硬件/品控问题 |

## 技术优势

- 真正的运营级输出：不仅在维度上覆盖全面，更给出可落地的改进建议
- 多语言支持：中文、英文、日语、韩语、西语等主流语言
- JSON解析容错：双重解析机制，保证99.9%+的正常输出率
- 与OpenAI兼容：可接入任何 OpenAI 兼容接口（DeepSeek、通义千问等）
```

### API 文档

#### 请求说明

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| product_name | string | 否 | 商品名称 |
| reviews | string[] | 是 | 用户评论列表 |
| max_reviews | integer | 否 | 最大分析条数（1-300），默认100 |

#### 请求示例

```json
{
  "product_name": "SoundCore Pro 真无线耳机",
  "reviews": [
    "降噪效果非常棒，地铁上用很安静",
    "续航很强，出差一周不用充电",
    "戴久了耳朵有点疼，耳套材质偏硬",
    "性价比没得说，这个价位无敌"
  ],
  "max_reviews": 10
}
```

#### 返回示例

```json
{
  "product_name": "SoundCore Pro 真无线耳机",
  "analyzed_count": 4,
  "total_count": 4,
  "analysis": {
    "strengths": [
      "降噪出色：'降噪效果非常棒，地铁上用很安静'",
      "续航强劲：'续航很强，出差一周不用充电'",
      "性价比高：'性价比没得说，这个价位无敌'"
    ],
    "weaknesses": [
      "佩戴舒适度不足（中）：'戴久了耳朵有点疼，耳套材质偏硬'"
    ],
    "improvement_suggestions": "优化耳套材质和人体工学设计，提升长时间佩戴舒适度",
    "user_profile": "注重降噪与续航的通勤/出差族，对性价比敏感，日常高频使用",
    "sentiment_distribution": {
      "positive": 0.75,
      "negative": 0.25,
      "neutral": 0
    }
  }
}
```

#### 错误码

| 状态码 | 说明 |
|---|---|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 认证失败 |
| 429 | 调用次数超限 |
| 500 | 服务内部错误 |
| 502 | 上游 LLM 服务不可用 |

### 套餐定价

| 套餐 | 价格 | 月调用次数 | 适用对象 |
|---|---|---|---|
| 免费版 | ¥0 | 20次 | 测试体验 |
| 基础版 | ¥69/月 | 500次 | 个人开发者/小店铺 |
| 专业版 | ¥199/月 | 2,000次 | 成长中的电商品牌 |
| 企业版 | ¥699/月 | 10,000次 | 代理机构/多品牌运营 |

### 服务协议

- 调用超时：120 秒
- 单次最大分析：300 条评论
- 数据隐私：评论数据仅内存处理，不落盘存储
- 可用性 SLA：99.5%（企业版 99.9%）

---

## 审核注意事项

1. API 必须有真实的公网后端地址，且可正常响应
2. 上传截图/视频展示 API 调用效果（提高通过率）
3. 服务描述中不要包含竞品名称（如 RapidAPI、Postman 等）
4. 定价不能为 0（免费版至少有调用次数限制）
5. 必须提供完整的错误码说明
